<div class="mx-auto mt-32 max-w-7xl px-6 sm:mt-40 lg:px-8 mb-24">
  <div class="mx-auto max-w-2xl lg:mx-0 lg:max-w-none">
    <h2 class="text-4xl font-semibold tracking-tight text-balance text-gray-900 sm:text-5xl dark:text-white">From the blog</h2>
    <p class="mt-2 text-lg/8 text-gray-600 dark:text-gray-400">Thoughts, tutorials, and updates on my coding journey.</p>
  </div>

  <div class="mx-auto mt-16 grid max-w-2xl auto-rows-fr grid-cols-1 gap-8 sm:mt-20 lg:mx-0 lg:max-w-none lg:grid-cols-3">

    {# --- Get 3 latest posts --- #}
    {% set latestPosts = collections.post | reverse %}
    {% for post in latestPosts.slice(0, 3) %}
        <article class="relative isolate flex flex-col justify-end overflow-hidden rounded-2xl bg-gray-900 px-8 pt-80 pb-8 sm:pt-48 lg:pt-80 dark:bg-gray-800 transition hover:scale-[1.01] duration-300">

        <div class="absolute inset-0 -z-10 bg-linear-to-t from-gray-900 via-gray-900/40 dark:from-black/80 dark:via-black/40"></div>
        <div class="absolute inset-0 -z-10 rounded-2xl inset-ring inset-ring-gray-900/10 dark:inset-ring-white/10"></div>

        <div class="flex flex-wrap items-center gap-y-1 overflow-hidden text-sm/6 text-gray-300">
            <time datetime="{{ post.date }}">{{ post.date | postDate }}</time>
        </div>
        <h3 class="mt-3 text-lg/6 font-semibold text-white">
            <a href="{{ post.url }}">
            <span class="absolute inset-0"></span>
            {{ post.data.title }}
            </a>
        </h3>
        </article>
    {% else %}
        <p class="text-gray-500">No blog posts found yet.</p>
    {% endfor %}

  </div>

  <div class="mt-12 text-center">
      <a href="/blog/" class="text-sm font-semibold leading-6 text-indigo-600 hover:text-indigo-500">View all posts <span aria-hidden="true">→</span></a>
  </div>
</div>