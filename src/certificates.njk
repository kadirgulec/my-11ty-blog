---
layout: base.njk
title: Certificates
description: A collection of my verified credentials, degrees, and continuous learning achievements.
---

<div class="mx-auto max-w-7xl px-6 pt-24 sm:pt-32 lg:px-8">
    <div class="mx-auto  lg:mx-0">
        <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl dark:text-white">Certificates</h2>
        <p class="mt-6 text-lg leading-8 text-gray-600 dark:text-gray-400">
            A collection of my verified credentials, degrees, and continuous learning achievements.
        </p>
    </div>

    <div class="mx-auto mt-16 grid  grid-cols-1 gap-6 sm:grid-cols-2 lg:mx-0 lg:max-w-none lg:grid-cols-3">
        {% for cert in certificates %}
        <!-- Reuse the card design here -->
        <div class="flex flex-col rounded-2xl border border-gray-200 bg-white p-8 shadow-sm transition hover:shadow-md dark:border-gray-700 dark:bg-gray-800">

            <div class="flex items-center gap-x-4">
                <div class="flex h-12 w-12 items-center justify-center rounded-lg bg-indigo-50 dark:bg-indigo-900/30">
                    {% if cert.logo != null %}
                        <img src="{{ cert.logo }}" alt="{{ cert.name }}" class="w-full h-full object-contain">
                    {% else %}
                        <span class="text-xl">📜</span>
                    {% endif %}
                </div>
                <div>
                    <div class="text-xs font-medium text-gray-500 dark:text-gray-400">{{ cert.issuer }}</div>
                    <div class="text-xs text-gray-400">{{ cert.date }}</div>
                </div>
            </div>

            <h3 class="mt-4 text-base font-semibold leading-7 tracking-tight text-gray-900 dark:text-white">
                {{ cert.name }}
            </h3>

            <div class="mt-4 flex flex-1 items-end">
                {% if cert.url and cert.url != '#' %}
                <a href="{{ cert.url }}" target="_blank"
                   class="button-a">
                    Verify Credential
                </a>
                {% endif %}
            </div>
        </div>
        {% endfor %}
    </div>
</div>